import React from "react";

const TitleComp = ({ veri }) => {
  return (
    <div className="section-heading">
      <span className="subheading">{veri}</span>
      <h3>{veri}</h3>
    </div>
  );
};

export default TitleComp;
